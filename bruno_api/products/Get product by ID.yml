info:
  name: Get product by ID
  type: http
  seq: 4

http:
  method: GET
  url: "{{app_url}}/{{v1_api}}/{{products_endpoint}}/{{product_id}}"
  headers:
    - name: Accept-Language
      value: "{{locale}}"
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{login_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
