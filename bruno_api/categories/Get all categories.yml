info:
  name: Get all categories
  type: http
  seq: 2

http:
  method: GET
  url: "{{app_url}}/{{v1_api}}/{{category_endpoint}}"
  headers:
    - name: Accept-Language
      value: "{{locale}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
