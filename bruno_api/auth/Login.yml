info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{app_url}}/{{v1_api}}/{{auth_endpoint}}/login"
  headers:
    - name: Accept-Language
      value: "{{locale}}"
  body:
    type: json
    data: |-
      {
        "email": "jane.doe2@example.com",
        "password": "securePass123"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setVar("login_token", res.body.data.token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
