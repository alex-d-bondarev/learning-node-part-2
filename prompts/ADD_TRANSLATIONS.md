Goal: Refactor validation messaging in [file path given by a user] to use locale strings via req.t().

Context/Constraint:
1. The source of truth for all messages is the files in the `locales/` directory. 
2. DO NOT DELETE any existing keys. If new keys are needed, you must add them.

Required Additions (Key: Values): [List specific key: value pairs for EN, RU, and UK.]

Process: Please perform the operation in three steps:
1. Acknowledge all files involved (routes/..., locales/..., etc.).
2. Provide a structured, text-only proposal showing exactly what will be written to each file (draft).
3. Wait for user explicit approval before executing the final update commands.
